{
  "name": "@symbo.ls/react-temp",
  "type": "commonjs",
  "main": "index.js",
  "dependencies": {
    "smbls": "latest"
  },
  "scripts": {
    "build:button": "npx esbuild **/*.js --target=es2020 --format=cjs --allow-overwrite --loader:.js=jsx --outdir=__dist/",
    "build": "npx esbuild **/*.js --target=es2020 --format=esm --allow-overwrite --loader:.js=jsx --outdir=."
  },
  "peerDependencies": {
    "domql": "latest",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
